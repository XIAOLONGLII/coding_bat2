def front_times(str, n):
  # method 1:
  return str[:3] * n
  # method 2:
  if len(str) >= 3:
    return str[:3] * n
  else:
    return str * n
  #method 3: 
  string = Word
  for x in range(n):
    return string[:3] * n
  
